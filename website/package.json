{
    "devDependencies": {
        "vitepress": "^1.3.2"
    },
    "scripts": {
        "docs:publish": "yarn docs:build && rm -rf ../public/doc-assets/* --yes && cp -rf ./.vitepress/dist/* ../public/",
        "docs:dev": "vitepress dev",
        "docs:build": "vitepress build",
        "docs:preview": "vitepress preview"
    }
}